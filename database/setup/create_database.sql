-- Create the database if it doesn't exist
-- Connect to the default 'postgres' database first, then run this

CREATE DATABASE "summitly-crm";

-- Grant privileges (optional, but recommended)
-- ALTER DATABASE "summitly-crm" OWNER TO postgres;
